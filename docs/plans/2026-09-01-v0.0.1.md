# dsh-engram v0.0.1 实现计划

> **For agentic workers:** 按任务顺序逐个执行，步骤使用 checkbox（`- [ ]`）语法跟踪。

**Goal:** 交付 dsh-engram v0.0.1（设计文档一期）：可安装的 DeepSeek Harness 记忆插件骨架 + SQLite 双库存储 + 本地中文嵌入 + 5 个基础工具 + 会话开始画像注入，全部带测试。

**Architecture:** 单插件包（billing 模式）：`apply` 注册 5 个 `engram_` 工具并挂 `agent/pre-step` waterfall 注入用户画像；存储层 `EngramStore` 接口 + node:sqlite 实现（节点表、边表、FTS5、操作日志，单调 SCHEMA_VERSION）；嵌入层 `EngramEmbedder` 接口 + transformers.js 实现，缺失或失败时检索降级 FTS-only。

**Tech Stack:** TypeScript (ESM, NodeNext)、Cordis 函数插件（`name`/`inject`/`Config`/`apply`）、`@deepseek-ai/dsh-tools` 的 `defineTool`、`node:sqlite`（动态 import）、`@huggingface/transformers`（`Xenova/bge-small-zh-v1.5`，512 维）、vitest、tsdown、pnpm。

**API 事实来源（已查证）：** 工具注册 `toolsCtx.tools.register(defineTool({...}))`（样板 `packages/client/ui-usage-billing/src/index.ts`）；dynamic context 注入走 `ctx.on('agent/pre-step', listener, { prepend: true })` + `createUserMessage({ source: { kind: 'plugin', plugin, form: 'snapshot', sections: [{ name, text }] } })`（样板 `packages/context/time-context/src/index.ts`）；`node:sqlite` 用 `const { DatabaseSync } = await import('node:sqlite')`（样板 `packages/session-query/session-query-sqlite/src/schema.ts`）。

**对设计骨架的两处裁剪（记入提交说明）：** ① `./invariant` 入口与 `src/presenter.ts` 独立文件移至二期——v0.0.1 的工具呈现走 `output.render` 纯文本渲染与客户端通用卡片；② 来源链 v0.0.1 只记 `sourceSessionId`（`exec.agent?.id`），`round`/`seq` 等自动摄取落地时补全。

---

### Task 1: 仓库骨架

**Files:**
- Create: `package.json`、`tsconfig.json`、`tsdown.config.ts`、`vitest.config.ts`、`.gitignore`、`cordis.patch.yml`、`README.md`

- [ ] **Step 1.1: 初始化依赖**

```sh
cd /Users/ken/dsh-engram
pnpm init
pnpm add @huggingface/transformers
pnpm add -D typescript tsdown vitest @types/node \
  @deepseek-ai/cordis @deepseek-ai/schemastery @deepseek-ai/dsh-tools \
  @deepseek-ai/dsh-llm @deepseek-ai/dsh-agent
```

预判失败与对策：① npm 上 `@deepseek-ai/*` 版本与 harness workspace 版本不一致 → 装完后 `pnpm ls @deepseek-ai/dsh-tools` 确认，peer 语义以 npm 实际版本为准；② 网络超时 → 换镜像或配置 `.npmrc` 代理；③ Node 版本不满足 `^22.19 || >=24` → `node -v` 确认后用 nvm 切换。

- [ ] **Step 1.2: 写 `package.json`**

在 pnpm 生成的文件上补齐以下字段（保留 pnpm 写入的实际依赖版本号）：

```json
{
  "name": "@kenz1117/dsh-engram",
  "description": "Cross-session long-term memory for DeepSeek Harness: dual-scope SQLite memory graph, hybrid retrieval, provenance audit, and a knowledge flywheel.",
  "version": "0.0.1",
  "publishConfig": { "access": "public" },
  "repository": { "type": "git", "url": "git+https://github.com/kenz1117/dsh-engram.git" },
  "type": "module",
  "main": "lib/index.js",
  "types": "lib/index.d.ts",
  "exports": {
    ".": { "types": "./lib/index.d.ts", "default": "./lib/index.js" },
    "./src/*": "./src/*",
    "./package.json": "./package.json"
  },
  "dsh": { "bundle": { "patch": "./cordis.patch.yml" } },
  "scripts": {
    "bundle": "tsdown",
    "watch": "tsdown --watch",
    "test": "vitest run",
    "typecheck": "tsc --noEmit"
  },
  "license": "MIT",
  "engines": { "node": "^22.19.0 || >=24.0.0" }
}
```

`@huggingface/transformers` 必须留在 `dependencies`（运行时经 profile node_modules 解析）；五个 `@deepseek-ai/*` 包最终应同时出现在 `peerDependencies` 与 `devDependencies`（npm 安装 peer 时不再拉错版本）。

- [ ] **Step 1.3: 写 `tsconfig.json`**

```json
{
  "compilerOptions": {
    "target": "ES2023",
    "module": "NodeNext",
    "moduleResolution": "NodeNext",
    "strict": true,
    "noImplicitAny": true,
    "verbatimModuleSyntax": true,
    "allowImportingTsExtensions": true,
    "rewriteRelativeImportExtensions": true,
    "declaration": true,
    "emitDeclarationOnly": true,
    "outDir": "lib/types",
    "rootDir": "src",
    "skipLibCheck": true,
    "types": ["node"]
  },
  "include": ["src"]
}
```

- [ ] **Step 1.4: 写 `tsdown.config.ts`**

```ts
import { defineConfig } from 'tsdown'

// host 半打包：workspace 与嵌入运行时依赖一律 external，
// lib/index.js 只含本包代码，依赖由 dsh plugin add 装进 profile node_modules。
export default defineConfig({
  entry: ['src/index.ts'],
  format: 'esm',
  platform: 'node',
  dts: true,
  external: [/^@deepseek-ai\//, /^@huggingface\//, /^onnxruntime/],
})
```

- [ ] **Step 1.5: 写 `vitest.config.ts`、`.gitignore`、`cordis.patch.yml`**

```ts
import { defineConfig } from 'vitest/config'

export default defineConfig({
  test: {
    environment: 'node',
    include: ['tests/**/*.spec.ts'],
    testTimeout: 30_000,
  },
})
```

```gitignore
node_modules/
lib/
*.log
```

```yaml
# dsh-engram bundle patch：dsh plugin add @kenz1117/dsh-engram 时以单条目插入 profile 根，
# 行 id 指向本插件 host 半（注册 engram_ 工具与画像注入）。
- insert:
    - id: dsh-engram
      name: '@kenz1117/dsh-engram'
```

- [ ] **Step 1.6: 验证**

```sh
pnpm typecheck   # 期望：无 src 报错（尚无源码时输出 "No inputs were found" 属预期，Task 2 后复验）
pnpm test        # 期望：No test files found（Task 3 后修复）
```

---

### Task 2: 类型与 Config

**Files:**
- Create: `src/types.ts`、`src/config.ts`
- Test: `tests/config.spec.ts`

- [ ] **Step 2.1: 写 `src/types.ts`**（只含类型，无运行时代码）

```ts
/**
 * dsh-engram 词汇表：记忆条目、关系边、查询与错误。
 * @module @kenz1117/dsh-engram/types
 */

/** 品牌化记忆 id：跨工具与存储边界的 id 一律此类型，拒绝裸 string。 */
declare const memoryIdBrand: unique symbol
export type MemoryId = string & { readonly [memoryIdBrand]: true }

/** 从任意字符串铸造品牌化 id（存储层入库前调用）。 */
export function asMemoryId(raw: string): MemoryId { return raw as MemoryId }

/** 记忆作用域：user 全局共享；project 按工作目录分库。 */
export type EngramScope = 'user' | 'project'

/** 记忆种类：fact 事实 / preference 偏好 / decision 决策 / episode 经历 / skill 方法。 */
export type EngramKind = 'fact' | 'preference' | 'decision' | 'episode' | 'skill'

/** 条目状态：active 参与检索；archived（衰减/被取代）不参与检索、可恢复；forgotten 软删、可恢复。 */
export type EngramStatus = 'active' | 'archived' | 'forgotten'

/** 关系边类型。supersedes 语义：from 取代 to。 */
export type EngramEdgeType = 'supports' | 'contradicts' | 'refines' | 'related' | 'supersedes'

/** 一条记忆。来源链 v0.0.1 记 sourceSessionId；round/seq 由二期自动摄取补全。 */
export interface MemoryRecord {
  readonly id: MemoryId
  readonly scope: EngramScope
  readonly kind: EngramKind
  readonly content: string
  /** 0-1：重要性，影响注入排序与衰减调度。 */
  readonly importance: number
  /** 0-1：置信度，命中强化提升、蒸馏失败回退。 */
  readonly confidence: number
  readonly status: EngramStatus
  readonly createdAt: number
  readonly lastAccessedAt: number
  readonly accessCount: number
  readonly sourceSessionId: string | null
}

/** 记忆关系边。 */
export interface MemoryEdge {
  readonly from: MemoryId
  readonly to: MemoryId
  readonly type: EngramEdgeType
  readonly createdAt: number
}

/** 写入请求。 */
export interface WriteInput {
  readonly scope: EngramScope
  readonly kind: EngramKind
  readonly content: string
  readonly importance?: number
  readonly confidence?: number
  readonly sourceSessionId?: string | null
}

/** 检索请求。 */
export interface SearchQuery {
  readonly text: string
  readonly scopes: readonly EngramScope[]
  readonly limit?: number
}

/** 一条检索命中。 */
export interface SearchHit {
  readonly record: MemoryRecord
  /** 融合得分（RRF），越高越相关。 */
  readonly score: number
  /** 该条命中的贡献道：fts 关键词 / vec 语义 / both。 */
  readonly via: 'fts' | 'vec' | 'both'
  /** 经关系边一跳扩展引入时，来源条目 id。 */
  readonly viaEdge?: { readonly from: MemoryId; readonly type: EngramEdgeType }
}

/** 检索结果：degraded=true 表示嵌入缺失/失败，仅关键词道参与排序。 */
export interface SearchResult {
  readonly hits: readonly SearchHit[]
  readonly degraded: boolean
}

/** 时间线查询。since/until 为 epoch 毫秒；topic 为子串匹配。 */
export interface TimelineQuery {
  readonly since?: number
  readonly until?: number
  readonly topic?: string
  readonly scopes: readonly EngramScope[]
  readonly limit?: number
}

/** 更新请求：旧条目转 archived 并建立 supersedes 边（from=新，to=旧）。 */
export interface UpdateInput {
  readonly id: MemoryId
  readonly scope: EngramScope
  readonly kind: EngramKind
  readonly content: string
  readonly importance?: number
}

/** dsh-engram 统一错误：加载/使用期的可诊断失败都抛此类型。 */
export class EngramError extends Error {
  /** 机器可读原因码，如 EMBEDDER_UNAVAILABLE / SCHEMA_INCOMPATIBLE。 */
  readonly code: string
  constructor(code: string, message: string, options?: { cause?: unknown }) {
    super(message, options)
    this.name = 'EngramError'
    this.code = code
  }
}
```

- [ ] **Step 2.2: 写 `src/config.ts`**

```ts
/**
 * 插件 Config：所有部署可变项集中于此，禁止在实现里内嵌默认值。
 * @module @kenz1117/dsh-engram/config
 */

import { homedir } from 'node:os'
import { join } from 'node:path'
import z from '@deepseek-ai/schemastery'

/** 插件配置。 */
export interface EngramConfig {
  /** 两个 SQLite 分库与嵌入模型缓存的根目录；默认 `~/.dsh/engram`。 */
  dbDir?: string
  /** 会话开始是否注入用户级画像摘要；默认 true。 */
  injectProfile?: boolean
  /** 画像注入的最大条数；默认 8。 */
  profileTopN?: number
  /** 嵌入模型缓存目录；默认 `<dbDir>/models`。 */
  modelCacheDir?: string
}

/** 解析后的完整配置（显式默认值集中在此一步，实现不再 `?? 默认`）。 */
export interface ResolvedEngramConfig {
  readonly dbDir: string
  readonly injectProfile: boolean
  readonly profileTopN: number
  readonly modelCacheDir: string
}

/** Schemastery 校验面（cordis.yml 读取时校验）。 */
export const Config: z<EngramConfig> = z.object({
  dbDir: z.string(),
  injectProfile: z.boolean(),
  profileTopN: z.number().int().min(1).max(64),
  modelCacheDir: z.string(),
})

/** 显式 resolve 步骤：默认值只在唯一的此处落地，非法值 loud 失败。 */
export function resolveConfig(config: EngramConfig = {}): ResolvedEngramConfig {
  if (config !== null && typeof config === 'object') {
    for (const key of Object.keys(config)) {
      if (!(key in Config['meta'] ?? {}) && !['dbDir', 'injectProfile', 'profileTopN', 'modelCacheDir'].includes(key)) {
        throw new Error(`dsh-engram: unknown config key "${key}"`)
      }
    }
  }
  const dbDir = config.dbDir ?? join(homedir(), '.dsh', 'engram')
  const injectProfile = config.injectProfile ?? true
  const profileTopN = config.profileTopN ?? 8
  if (config.profileTopN !== undefined && (!Number.isInteger(config.profileTopN) || config.profileTopN < 1 || config.profileTopN > 64)) {
    throw new Error('dsh-engram: profileTopN must be an integer in [1, 64]')
  }
  return { dbDir, injectProfile, profileTopN, modelCacheDir: config.modelCacheDir ?? join(dbDir, 'models') }
}
```

- [ ] **Step 2.3: 写 `tests/config.spec.ts`**

```ts
import { homedir } from 'node:os'
import { join } from 'node:path'
import { describe, expect, it } from 'vitest'
import { resolveConfig } from '../src/config.ts'

describe('resolveConfig', () => {
  it('空配置回落到 ~/.dsh/engram 与全部默认值', () => {
    const resolved = resolveConfig({})
    expect(resolved.dbDir).toBe(join(homedir(), '.dsh', 'engram'))
    expect(resolved.injectProfile).toBe(true)
    expect(resolved.profileTopN).toBe(8)
    expect(resolved.modelCacheDir).toBe(join(homedir(), '.dsh', 'engram', 'models'))
  })

  it('显式值全部透传', () => {
    const resolved = resolveConfig({ dbDir: '/tmp/e', injectProfile: false, profileTopN: 3, modelCacheDir: '/tmp/m' })
    expect(resolved).toEqual({ dbDir: '/tmp/e', injectProfile: false, profileTopN: 3, modelCacheDir: '/tmp/m' })
  })

  it('未知键 loud 失败', () => {
    expect(() => resolveConfig({ nope: 1 } as never)).toThrow(/unknown config key/)
  })

  it('profileTopN 越界 loud 失败', () => {
    expect(() => resolveConfig({ profileTopN: 0 })).toThrow(/profileTopN/)
    expect(() => resolveConfig({ profileTopN: 65 })).toThrow(/profileTopN/)
  })
})
```

- [ ] **Step 2.4: 运行验证**

```sh
pnpm typecheck && pnpm test -- tests/config.spec.ts   # 期望：4 passed
```

- [ ] **Step 2.5: 提交**

```sh
git add -A && git commit -m "feat: config and vocabulary types for dsh-engram"
```

---

### Task 3: EngramStore 接口与 SQLite 实现

**Files:**
- Create: `src/store/interface.ts`、`src/store/sqlite.ts`
- Test: `tests/store.spec.ts`

- [ ] **Step 3.1: 写 `src/store/interface.ts`**

```ts
/**
 * EngramStore：存储 Provider 的可替换接口（capability seam 的 Provider 角色）。
 * 实现必须满足：节点/边/操作日志持久化、scope 分库由调用方持有两个实例。
 * @module @kenz1117/dsh-engram/store/interface
 */

import type { MemoryId, MemoryRecord, SearchQuery, SearchResult, TimelineQuery, UpdateInput, WriteInput } from '../types.ts'

/** 写入返回新条目。 */
export interface EngramStore {
  /** 追加一条 active 记忆，content 非空。 */
  write(input: WriteInput): Promise<MemoryRecord>
  /** 按 id 取条目；不存在返回 undefined。 */
  get(id: MemoryId): Promise<MemoryRecord | undefined>
  /** 混合检索（向量 + FTS5 + 一跳扩展）。 */
  search(query: SearchQuery, embeddings: Float32Array | undefined): Promise<SearchResult>
  /** 时间线查询，按 createdAt 倒序。 */
  timeline(query: TimelineQuery): Promise<MemoryRecord[]>
  /** 修正：旧条目转 archived，建立 supersedes 边，返回新条目。 */
  update(input: UpdateInput, embeddings: Float32Array | undefined): Promise<MemoryRecord>
  /** 软删（可恢复）。 */
  forget(id: MemoryId): Promise<MemoryRecord>
  /** 从 archived/forgotten 恢复为 active。 */
  restore(id: MemoryId): Promise<MemoryRecord>
  /** 画像注入：active 条目按 importance、confidence 倒序取前 n。 */
  topActive(scope: 'user', n: number): Promise<MemoryRecord[]>
  /** 物理清除指定 scope 全部数据（导出后使用）。 */
  purge(): Promise<void>
  /** 关闭数据库句柄。 */
  close(): Promise<void>
}
```

- [ ] **Step 3.2: 写 `tests/store.spec.ts`**（先失败）

```ts
import { mkdtemp } from 'node:fs/promises'
import { tmpdir } from 'node:os'
import { join } from 'node:path'
import { afterEach, beforeEach, describe, expect, it } from 'vitest'
import { openEngramStore } from '../src/store/sqlite.ts'
import { asMemoryId } from '../src/types.ts'
import type { EngramStore } from '../src/store/interface.ts'

let dir: string
let store: EngramStore

beforeEach(async () => { dir = await mkdtemp(join(tmpdir(), 'engram-')); store = await openEngramStore(join(dir, 'user.db')) })
afterEach(async () => { await store.close() })

describe('EngramStore (sqlite)', () => {
  it('write 后可 get，字段完整', async () => {
    const record = await store.write({ scope: 'user', kind: 'preference', content: '用户偏好简体中文回复', importance: 0.8 })
    const fetched = await store.get(record.id)
    expect(fetched?.content).toBe('用户偏好简体中文回复')
    expect(fetched?.status).toBe('active')
    expect(fetched?.accessCount).toBe(0)
    expect(fetched?.sourceSessionId).toBeNull()
  })

  it('FTS 检索命中关键词', async () => {
    await store.write({ scope: 'user', kind: 'fact', content: '项目使用 pnpm 管理依赖' })
    const result = await store.search({ text: 'pnpm', scopes: ['user'] }, undefined)
    expect(result.degraded).toBe(true)
    expect(result.hits.length).toBe(1)
    expect(result.hits[0]!.via).toBe('fts')
  })

  it('向量道与 FTS 道融合，degraded=false', async () => {
    await store.write({ scope: 'user', kind: 'fact', content: ' alpha 内容 ' })
    // 确定性伪向量：测试用 query 向量与首条记录同向。
    const vec = new Float32Array(512).fill(0.1)
    const result = await store.search({ text: 'alpha', scopes: ['user'] }, vec)
    expect(result.degraded).toBe(false)
    expect(result.hits[0]!.via).toBe('both')
  })

  it('update 建立 supersedes 边且旧条目退出检索', async () => {
    const old = await store.write({ scope: 'user', kind: 'fact', content: '端口是 3000' })
    const next = await store.update({ id: old.id, scope: 'user', kind: 'fact', content: '端口改为 4000' }, undefined)
    expect((await store.get(old.id))?.status).toBe('archived')
    const result = await store.search({ text: '端口', scopes: ['user'] }, undefined)
    expect(result.hits.map(hit => hit.record.id)).toEqual([next.id])
  })

  it('forget/restore 往返', async () => {
    const record = await store.write({ scope: 'user', kind: 'fact', content: '待删除条目' })
    expect((await store.forget(record.id)).status).toBe('forgotten')
    expect(await store.get(record.id)).toBeDefined()
    expect((await store.restore(record.id)).status).toBe('active')
  })

  it('topActive 按 importance 倒序', async () => {
    await store.write({ scope: 'user', kind: 'fact', content: '低重要性', importance: 0.2 })
    await store.write({ scope: 'user', kind: 'fact', content: '高重要性', importance: 0.9 })
    const top = await store.topActive('user', 1)
    expect(top[0]!.content).toBe('高重要性')
  })

  it('purge 物理清空', async () => {
    await store.write({ scope: 'user', kind: 'fact', content: '将被清除' })
    await store.purge()
    const result = await store.search({ text: '清除', scopes: ['user'] }, undefined)
    expect(result.hits).toHaveLength(0)
  })

  it('schema 版本不兼容时拒绝打开', async () => {
    const path = join(dir, 'bad.db')
    const bad = await openEngramStore(path)
    await bad.close()
    // 直接改写 meta 里的 schema 版本模拟旧版本库。
    const { DatabaseSync } = await import('node:sqlite')
    const db = new DatabaseSync(path)
    db.prepare("UPDATE meta SET value = '999' WHERE key = 'schema_version'").run()
    db.close()
    await expect(openEngramStore(path)).rejects.toThrow(/schema/i)
  })

  it('asMemoryId 品牌化 id 可透传 get', async () => {
    const record = await store.write({ scope: 'user', kind: 'fact', content: '品牌 id' })
    expect((await store.get(asMemoryId(record.id)))?.id).toBe(record.id)
  })
})
```

- [ ] **Step 3.3: 运行验证测试失败**

```sh
pnpm test -- tests/store.spec.ts   # 期望：FAIL，openEngramStore 不存在
```

- [ ] **Step 3.4: 写 `src/store/sqlite.ts`**

```ts
/**
 * EngramStore 的 node:sqlite 实现：节点表 + 边表 + trigram FTS5 + 操作日志，
 * 单调 SCHEMA_VERSION，打开时校验、不兼容拒绝加载（不写兼容 shim）。
 * @module @kenz1117/dsh-engram/store/sqlite
 */

import { mkdir, readFile } from 'node:fs/promises'
import { dirname } from 'node:path'
import { randomUUID } from 'node:crypto'
import type { DatabaseSync } from 'node:sqlite'
import { EngramError, asMemoryId } from '../types.ts'
import type { MemoryId, MemoryRecord, MemoryEdge, SearchHit, SearchResult, TimelineQuery, UpdateInput, WriteInput, EngramScope } from '../types.ts'
import type { EngramStore } from './interface.ts'

/** 当前 schema 版本；任何结构性变更都必须 +1 并拒绝旧库（pre-release 无兼容承诺）。 */
const SCHEMA_VERSION = 1
/** FTS 道与向量道融合的 RRF 常数。 */
const RRF_K = 60
/** 向量道扫描的候选上限：active 且带向量的条目全量参与余弦计算。 */

interface NodeRow {
  id: string; scope: string; kind: string; content: string
  importance: number; confidence: number; status: string
  created_at: number; last_accessed_at: number; access_count: number
  source_session_id: string | null; embedding: Uint8Array | null
}

function rowToRecord(row: NodeRow): MemoryRecord {
  return {
    id: asMemoryId(row.id), scope: row.scope as EngramScope, kind: row.kind as MemoryRecord['kind'],
    content: row.content, importance: row.importance, confidence: row.confidence, status: row.status as MemoryRecord['status'],
    createdAt: row.created_at, lastAccessedAt: row.last_accessed_at, accessCount: row.access_count,
    sourceSessionId: row.source_session_id,
  }
}

function vecToBlob(vector: Float32Array): Buffer { return Buffer.from(vector.buffer, vector.byteOffset, vector.byteLength) }
function blobToVec(blob: Uint8Array): Float32Array { return new Float32Array(blob.buffer.slice(blob.byteOffset, blob.byteOffset + blob.byteLength)) }

/** 余弦相似度（嵌入已归一化时等价于点积；仍按完整公式防御未归一化向量）。 */
function cosine(a: Float32Array, b: Float32Array): number {
  let dot = 0; let na = 0; let nb = 0
  const len = Math.min(a.length, b.length)
  for (let i = 0; i < len; i++) { dot += a[i]! * b[i]!; na += a[i]! * a[i]!; nb += b[i]! * b[i]! }
  if (na === 0 || nb === 0) return 0
  return dot / (Math.sqrt(na) * Math.sqrt(nb))
}

/**
 * 打开（必要时创建）一个 scope 分库。
 * @param path - SQLite 文件路径；目录不存在会自动创建（0o700）。
 * @returns 就绪的 EngramStore。
 * @throws EngramError(code=SCHEMA_INCOMPATIBLE) 当库版本高于当前实现。
 */
export async function openEngramStore(path: string): Promise<EngramStore> {
  await mkdir(dirname(path), { recursive: true, mode: 0o700 })
  const { DatabaseSync } = await import('node:sqlite')
  const db = new DatabaseSync(path)
  db.exec(`
    CREATE TABLE IF NOT EXISTS meta (key TEXT PRIMARY KEY, value TEXT NOT NULL);
    CREATE TABLE IF NOT EXISTS nodes (
      id TEXT PRIMARY KEY, scope TEXT NOT NULL, kind TEXT NOT NULL, content TEXT NOT NULL,
      importance REAL NOT NULL, confidence REAL NOT NULL, status TEXT NOT NULL,
      created_at INTEGER NOT NULL, last_accessed_at INTEGER NOT NULL, access_count INTEGER NOT NULL,
      source_session_id TEXT, embedding BLOB);
    CREATE TABLE IF NOT EXISTS edges (
      from_id TEXT NOT NULL, to_id TEXT NOT NULL, type TEXT NOT NULL, created_at INTEGER NOT NULL,
      PRIMARY KEY (from_id, to_id, type));
    CREATE TABLE IF NOT EXISTS op_log (
      seq INTEGER PRIMARY KEY AUTOINCREMENT, at INTEGER NOT NULL, op TEXT NOT NULL, target_id TEXT NOT NULL, detail TEXT);
    CREATE VIRTUAL TABLE IF NOT EXISTS nodes_fts USING fts5(node_id UNINDEXED, content, tokenize='trigram');
    CREATE INDEX IF NOT EXISTS nodes_status ON nodes (scope, status);
  `)
  const versionRow = db.prepare("SELECT value FROM meta WHERE key = 'schema_version'").get() as { value: string } | undefined
  if (versionRow === undefined) {
    db.prepare("INSERT INTO meta (key, value) VALUES ('schema_version', ?)").run(String(SCHEMA_VERSION))
  } else if (Number(versionRow.value) > SCHEMA_VERSION) {
    db.close()
    throw new EngramError('SCHEMA_INCOMPATIBLE', `engram 数据库 schema 版本 ${versionRow.value} 高于插件支持的 ${SCHEMA_VERSION}，请升级插件`)
  }

  const now = (): number => Date.now()
  const log = db.prepare('INSERT INTO op_log (at, op, target_id, detail) VALUES (?, ?, ?, ?)')
  const insertNode = db.prepare(`INSERT INTO nodes
    (id, scope, kind, content, importance, confidence, status, created_at, last_accessed_at, access_count, source_session_id, embedding)
    VALUES (?, ?, ?, ?, ?, ?, 'active', ?, ?, 0, ?, ?)`)
  const insertFts = db.prepare('INSERT INTO nodes_fts (node_id, content) VALUES (?, ?)')
  const deleteFts = db.prepare('DELETE FROM nodes_fts WHERE node_id = ?')

  const recordOps = {
    get: db.prepare('SELECT * FROM nodes WHERE id = ?'),
    activeRows: db.prepare("SELECT * FROM nodes WHERE scope = ? AND status = 'active' AND embedding IS NOT NULL"),
    byIdIn: db.prepare('SELECT * FROM nodes WHERE id = ?'),
    ftsSearch: db.prepare(`SELECT n.*, bm25(nodes_fts) AS rank FROM nodes_fts f JOIN nodes n ON n.id = f.node_id
      WHERE nodes_fts MATCH ? AND n.status = 'active' AND n.scope = ? ORDER BY rank LIMIT 64`),
    setStatus: db.prepare('UPDATE nodes SET status = ?, last_accessed_at = ? WHERE id = ?'),
    touch: db.prepare('UPDATE nodes SET access_count = access_count + 1, last_accessed_at = ? WHERE id = ?'),
    topActive: db.prepare("SELECT * FROM nodes WHERE scope = 'user' AND status = 'active' ORDER BY importance DESC, confidence DESC LIMIT ?"),
    timeline: db.prepare(`SELECT * FROM nodes WHERE scope = ? AND status = 'active'
      AND (? IS NULL OR created_at >= ?) AND (? IS NULL OR created_at <= ?)
      AND (? IS NULL OR content LIKE '%' || ? || '%') ORDER BY created_at DESC LIMIT ?`),
    purgeAll: db.prepare('DELETE FROM nodes'),
    purgeEdges: db.prepare('DELETE FROM edges'),
    purgeFts: db.prepare("DELETE FROM nodes_fts"),
    purgeLog: db.prepare('DELETE FROM op_log'),
    upsertEdge: db.prepare('INSERT OR IGNORE INTO edges (from_id, to_id, type, created_at) VALUES (?, ?, ?, ?)'),
    neighbors: db.prepare(`SELECT * FROM edges WHERE from_id IN (SELECT value FROM json_each(?))
      AND type IN ('supports','refines','related')`),
  }

  const getRow = (id: string): NodeRow | undefined => recordOps.get.get(id) as NodeRow | undefined

  return {
    async write(input: WriteInput) {
      const content = input.content.trim()
      if (content === '') throw new EngramError('EMPTY_CONTENT', 'content 不能为空')
      const id = asMemoryId(randomUUID())
      const at = now()
      db.transaction(() => {
        insertNode.run(id, input.scope, input.kind, content, input.importance ?? 0.5, input.confidence ?? 0.5, at, at, input.sourceSessionId ?? null, null)
        insertFts.run(id, content)
        log.run(at, 'write', id, JSON.stringify({ kind: input.kind, scope: input.scope }))
      })()
      return rowToRecord(getRow(id)!)
    },

    async get(id: MemoryId) {
      const row = getRow(id)
      return row === undefined ? undefined : rowToRecord(row)
    },

    async search(query, embeddings) {
      const limit = query.limit ?? 8
      const scores = new Map<string, { score: number; via: SearchHit['via'] }>()
      // 道 1：FTS5 trigram 关键词。
      const ftsRows = db.prepare(`SELECT n.*, bm25(nodes_fts) AS rank FROM nodes_fts f JOIN nodes n ON n.id = f.node_id
        WHERE nodes_fts MATCH ? AND n.status = 'active' AND n.scope IN (${query.scopes.map(() => '?').join(',') || "''"})
        ORDER BY rank LIMIT 64`).all(query.text.replace(/["'()*]/g, ' '), ...query.scopes) as (NodeRow & { rank: number })[]
      ftsRows.forEach((row, index) => {
        scores.set(row.id, { score: 1 / (RRF_K + index + 1), via: 'fts' })
      })
      // 道 2：向量余弦（全量 active 带向量条目）。
      let degraded = true
      if (embeddings !== undefined) {
        degraded = false
        const vecRows = db.prepare(`SELECT * FROM nodes WHERE status = 'active'
          AND embedding IS NOT NULL AND scope IN (${query.scopes.map(() => '?').join(',') || "''"})`).all(...query.scopes) as NodeRow[]
        const vecRanked = vecRows
          .map(row => ({ row, sim: cosine(embeddings, blobToVec(row.embedding!)) }))
          .filter(entry => entry.sim > 0.2)
          .sort((a, b) => b.sim - a.sim)
          .slice(0, 64)
        vecRanked.forEach((entry, index) => {
          const existing = scores.get(entry.row.id)
          const add = 1 / (RRF_K + index + 1)
          if (existing === undefined) scores.set(entry.row.id, { score: add, via: 'vec' })
          else scores.set(entry.row.id, { score: existing.score + add, via: 'both' })
        })
      }
      // 一跳关系扩展：supports/refines/related 邻居若 active 且未入结果，低权重引入。
      const topIds = [...scores.entries()].sort((a, b) => b[1].score - a[1].score).slice(0, limit).map(([id]) => id)
      if (topIds.length > 0) {
        const edges = recordOps.neighbors.all(JSON.stringify(topIds)) as MemoryEdge[]
        for (const edge of edges.slice(0, 32)) {
          if (scores.has(edge.to) || topIds.includes(edge.to)) continue
          const row = getRow(edge.to)
          if (row === undefined || row.status !== 'active' || !query.scopes.includes(row.scope as EngramScope)) continue
          scores.set(edge.to, { score: scores.get(edge.from)!.score * 0.5, via: 'fts' })
          void 0
        }
      }
      // 排序截断 + 命中强化 + viaEdge 回填。
      const finalRows = [...scores.entries()].sort((a, b) => b[1].score - a[1].score).slice(0, limit)
      const hits: SearchHit[] = []
      for (const [id, info] of finalRows) {
        const row = getRow(id)!
        hits.push({ record: rowToRecord(row), score: info.score, via: info.via })
        recordOps.touch.run(now(), id)
      }
      return { hits, degraded }
    },

    async timeline(query: TimelineQuery) {
      const limit = query.limit ?? 20
      const rows = db.prepare(`SELECT * FROM nodes WHERE status = 'active' AND scope IN (${query.scopes.map(() => '?').join(',') || "''"})
        AND (? IS NULL OR created_at >= ?) AND (? IS NULL OR created_at <= ?)
        AND (? IS NULL OR content LIKE '%' || ? || '%') ORDER BY created_at DESC LIMIT ?`)
        .all(...query.scopes, query.since ?? null, query.since ?? null, query.until ?? null, query.until ?? null, query.topic ?? null, query.topic ?? null, limit) as NodeRow[]
      return rows.map(rowToRecord)
    },

    async update(input: UpdateInput, embeddings) {
      const old = getRow(input.id)
      if (old === undefined) throw new EngramError('NOT_FOUND', `条目 ${input.id} 不存在`)
      const content = input.content.trim()
      if (content === '') throw new EngramError('EMPTY_CONTENT', 'content 不能为空')
      const id = asMemoryId(randomUUID())
      const at = now()
      db.transaction(() => {
        recordOps.setStatus.run('archived', at, input.id)
        insertNode.run(id, input.scope, input.kind, content, input.importance ?? old.importance, old.confidence, at, at, old.source_session_id, embeddings === undefined ? null : vecToBlob(embeddings))
        insertFts.run(id, content)
        recordOps.upsertEdge.run(id, input.id, 'supersedes', at)
        log.run(at, 'update', id, JSON.stringify({ supersedes: input.id }))
      })()
      return rowToRecord(getRow(id)!)
    },

    async forget(id: MemoryId) {
      const row = getRow(id)
      if (row === undefined) throw new EngramError('NOT_FOUND', `条目 ${id} 不存在`)
      recordOps.setStatus.run('forgotten', now(), id)
      log.run(now(), 'forget', id, null)
      return rowToRecord(getRow(id)!)
    },

    async restore(id: MemoryId) {
      const row = getRow(id)
      if (row === undefined) throw new EngramError('NOT_FOUND', `条目 ${id} 不存在`)
      recordOps.setStatus.run('active', now(), id)
      log.run(now(), 'restore', id, null)
      return rowToRecord(getRow(id)!)
    },

    async topActive(_scope: 'user', n: number) {
      const rows = recordOps.topActive.all(n) as NodeRow[]
      return rows.map(rowToRecord)
    },

    async purge() {
      db.transaction(() => {
        recordOps.purgeAll.run(); recordOps.purgeEdges.run(); recordOps.purgeFts.run(); recordOps.purgeLog.run()
      })()
      log.run(now(), 'purge', 'ALL', null)
    },

    async close() { db.close() },
  }
}

/** 嵌入向量落盘助手：update 路径与二期摄取共用。 */
export { vecToBlob, blobToVec }
```

注意两点修正（实现时直接写对）：① `recordOps` 里预编译的 `ftsSearch`/`timeline` 未被 search 使用（search 内联了 scope IN 列表版本），删除未用的预编译项，避免死代码；② 一跳扩展里 `scores.get(edge.from)!.score` 的 `edge.from` 可能不在 topIds（json_each 展开的键是 from_id，恒在 topIds 内），保留 `!` 并补注释说明该不变量。

- [ ] **Step 3.5: 运行验证**

```sh
pnpm test -- tests/store.spec.ts   # 期望：9 passed
```

- [ ] **Step 3.6: 提交**

```sh
git add -A && git commit -m "feat: sqlite-backed EngramStore with FTS5, edges, and op log"
```

---

### Task 4: EngramEmbedder 接口与本地实现

**Files:**
- Create: `src/embedder/interface.ts`、`src/embedder/local.ts`
- Test: `tests/embedder.spec.ts`

- [ ] **Step 4.1: 写 `src/embedder/interface.ts`**

```ts
/**
 * EngramEmbedder：嵌入 Provider 的可替换接口。实现负责池化与归一化。
 * @module @kenz1117/dsh-engram/embedder/interface
 */

/** 单条嵌入输出。 */
export interface Embedding {
  /** L2 归一化后的向量（bge-small-zh-v1.5 为 512 维）。 */
  readonly vector: Float32Array
}

/** 嵌入 Provider 接口。 */
export interface EngramEmbedder {
  /** 批量嵌入；实现可内部分批。 */
  embed(texts: readonly string[]): Promise<Embedding[]>
  /** 模型标识（写入诊断与结果元数据）。 */
  readonly model: string
}
```

- [ ] **Step 4.2: 写 `tests/embedder.spec.ts`**

```ts
import { mkdtemp } from 'node:fs/promises'
import { tmpdir } from 'node:os'
import { join } from 'node:path'
import { describe, expect, it } from 'vitest'
import { createLocalEmbedder } from '../src/embedder/local.ts'

// 需要下载真实模型（约 50MB，quantized），仅在有网时执行：ENGRAM_E2E=1 pnpm test
const shouldRun = process.env.ENGRAM_E2E === '1'
describe.runIf(shouldRun)('createLocalEmbedder (e2e)', () => {
  it('产出 512 维归一化向量', async () => {
    const dir = await mkdtemp(join(tmpdir(), 'engram-model-'))
    const embedder = await createLocalEmbedder(dir)
    const [first, second] = await embedder.embed(['今天天气很好', '天气晴朗'])
    expect(first!.vector.length).toBe(512)
    const norm = Math.sqrt(first!.vector.reduce((sum, v) => sum + v * v, 0))
    expect(norm).toBeCloseTo(1, 1)
    // 语义相近句子相似度应明显高于与随机第三句。
    const [third] = await embedder.embed('正弦函数的导数是余弦函数')
    const dot = (a: Float32Array, b: Float32Array): number => a.reduce((sum, v, i) => sum + v * b[i]!, 0)
    expect(dot(first!.vector, second!.vector)).toBeGreaterThan(dot(first!.vector, third!.vector))
    await embedder.close()
  }, 120_000)
})
```

- [ ] **Step 4.3: 写 `src/embedder/local.ts`**

```ts
/**
 * transformers.js 本地嵌入实现：Xenova/bge-small-zh-v1.5，量化权重，离线推理。
 * 模型首次使用需联网下载到 cacheDir；之后完全离线。
 * @module @kenz1117/dsh-engram/embedder/local
 */

import { mkdir } from 'node:fs/promises'
import type { EngramEmbedder } from './interface.ts'
import { EngramError } from '../types.ts'

/** 单例管线：进程内重复调用 createLocalEmbedder 共享同一模型实例。 */
let cached: { dir: string; embedder: EngramEmbedder } | undefined

/** 释放共享管线（测试隔离用；生产无需调用）。 */
export async function resetEmbedderCache(): Promise<void> { cached = undefined }

/**
 * 创建本地嵌入器。
 * @param cacheDir - 模型缓存目录；不存在会自动创建。
 * @returns 就绪的 EngramEmbedder。
 * @throws EngramError(code=EMBEDDER_DOWNLOAD_FAILED) 模型下载/加载失败。
 */
export async function createLocalEmbedder(cacheDir: string): Promise<EngramEmbedder> {
  if (cached?.dir === cacheDir) return cached.embedder
  await mkdir(cacheDir, { recursive: true, mode: 0o700 })
  try {
    const { pipeline, env } = await import('@huggingface/transformers')
    env.cacheDir = cacheDir
    const extractor = await pipeline('feature-extraction', 'Xenova/bge-small-zh-v1.5', { dtype: 'q8' })
    const embedder: EngramEmbedder = {
      model: 'Xenova/bge-small-zh-v1.5',
      async embed(texts) {
        if (texts.length === 0) return []
        const output = await extractor(texts as unknown as string[], { pooling: 'mean', normalize: true })
        const lists = output.tolist() as number[][]
        return lists.map(list => new Float32Array(list))
      },
      // 管线由模块级 cached 持有，close 仅解除本实例引用。
      close: async () => { if (cached?.embedder === embedder) cached = undefined },
    }
    cached = { dir: cacheDir, embedder }
    return embedder
  } catch (error) {
    throw new EngramError('EMBEDDER_DOWNLOAD_FAILED', '嵌入模型下载/加载失败；检索已降级为纯关键词模式，可检查网络后重试', { cause: error })
  }
}
```

- [ ] **Step 4.4: 运行验证**

```sh
pnpm typecheck && pnpm test   # 期望：已有 spec 全绿；e2e 默认 skip
```

- [ ] **Step 4.5: 提交**

```sh
git add -A && git commit -m "feat: local bge-small-zh embedder with download-failure degradation"
```

---

### Task 5: 插件入口与 5 个工具

**Files:**
- Create: `src/tools/create.ts`、`src/index.ts`
- Test: `tests/tools.spec.ts`

- [ ] **Step 5.1: 写 `tests/tools.spec.ts`**（先失败）

```ts
import { mkdtemp } from 'node:fs/promises'
import { tmpdir } from 'node:os'
import { join } from 'node:path'
import { afterEach, beforeEach, describe, expect, it } from 'vitest'
import { openEngramStore } from '../src/store/sqlite.ts'
import { createEngramTools } from '../src/tools/create.ts'
import type { EngramStore } from '../src/store/interface.ts'

let store: EngramStore
let tools: Map<string, { execute: (args: unknown, exec: unknown) => Promise<unknown> }>

beforeEach(async () => {
  const dir = await mkdtemp(join(tmpdir(), 'engram-tools-'))
  store = await openEngramStore(join(dir, 'user.db'))
  const list = createEngramTools({ openStore: async () => store, embedder: undefined })
  tools = new Map(list.map(tool => [tool.name, tool as never]))
})
afterEach(async () => { await store.close() })

const fakeExec = { agent: { id: 'sess-1' }, signal: new AbortController().signal }

describe('engram tools', () => {
  it('engram_save 写入并回显来源会话', async () => {
    const result = await tools.get('engram_save')!.execute(
      { content: '用户偏好深色主题', kind: 'preference', importance: 0.7, scope: 'user' }, fakeExec)
    expect((result as { record: { sourceSessionId: string } }).record.sourceSessionId).toBe('sess-1')
  })

  it('engram_search 返回命中与 degraded 标记', async () => {
    await store.write({ scope: 'user', kind: 'fact', content: '部署在 4000 端口' })
    const result = await tools.get('engram_search')!.execute({ query: '端口', scope: 'user' }, fakeExec) as { degraded: boolean; text: string }
    expect(result.degraded).toBe(true)
    expect(result.text).toContain('4000')
  })

  it('engram_update 走 supersedes 链', async () => {
    const saved = await store.write({ scope: 'user', kind: 'decision', content: '选 pnpm' })
    const result = await tools.get('engram_update')!.execute({ id: saved.id, content: '改用 npm', scope: 'user', kind: 'decision' }, fakeExec) as { record: { id: string }; superseded: string }
    expect(result.record.id).not.toBe(saved.id)
    expect(result.superseded).toBe(saved.id)
  })

  it('engram_forget 后 engram_search 不再命中', async () => {
    const saved = await store.write({ scope: 'user', kind: 'fact', content: '临时令牌 abc123' })
    await tools.get('engram_forget')!.execute({ id: saved.id }, fakeExec)
    const result = await tools.get('engram_search')!.execute({ query: '令牌', scope: 'user' }, fakeExec) as { text: string }
    expect(result.text).not.toContain('abc123')
  })

  it('engram_timeline 返回时间倒序', async () => {
    const a = await store.write({ scope: 'user', kind: 'episode', content: '事件 A' })
    const b = await store.write({ scope: 'user', kind: 'episode', content: '事件 B' })
    const result = await tools.get('engram_timeline')!.execute({ scope: 'user' }, fakeExec) as { text: string }
    expect(result.text.indexOf(b.id)).toBeLessThan(result.text.indexOf(a.id))
  })

  it('嵌入缺失时检索结果带降级说明', async () => {
    const result = await tools.get('engram_search')!.execute({ query: '任意', scope: 'user' }, fakeExec) as { degraded: boolean }
    expect(result.degraded).toBe(true)
  })
})
```

- [ ] **Step 5.2: 写 `src/tools/create.ts`**

```ts
/**
 * 5 个 engram_ 工具的定义与执行器。工具 schema 保持窄参数；
 * scope 决定读写哪个分库；嵌入缺失时检索结果显式标记降级。
 * @module @kenz1117/dsh-engram/tools/create
 */

import type { ToolDefinition } from '@deepseek-ai/dsh-tools'
import { defineTool } from '@deepseek-ai/dsh-tools'
import type { EngramStore } from '../store/interface.ts'
import type { EngramEmbedder } from '../embedder/interface.ts'
import type { EngramScope } from '../types.ts'

/** 工具依赖：懒打开的分库与可选嵌入器。 */
export interface ToolDeps {
  /** 每次调用解析目标 scope 的分库（user/project 各一）。 */
  readonly openStore: (scope: EngramScope) => Promise<EngramStore>
  /** 可选嵌入器；undefined = 降级纯关键词检索。 */
  readonly embedder: EngramEmbedder | undefined
}

/** 从模型参数收敛 scope（缺失回 project）。 */
function scopeOf(raw: unknown, fallback: EngramScope = 'project'): EngramScope {
  return raw === 'user' ? 'user' : raw === 'project' ? 'project' : fallback
}

/**
 * 构造 5 个工具定义（engram_save/search/timeline/update/forget）。
 * @param deps - 分库打开器与嵌入器。
 * @returns 可直接 register 的工具定义数组。
 */
export function createEngramTools(deps: ToolDeps): ToolDefinition[] {
  const save = defineTool({
    name: 'engram_save',
    description: '保存一条长期记忆（跨会话可用）。kind：fact 事实 / preference 偏好 / decision 决策 / episode 经历 / skill 方法。scope：project 仅当前项目，user 全局。',
    parameters: {
      content: { type: 'string', required: true, description: '记忆正文，一句话完整表达' },
      kind: { type: 'string', enum: ['fact', 'preference', 'decision', 'episode', 'skill'], required: true, description: '记忆种类' },
      scope: { type: 'string', enum: ['user', 'project'], description: '作用域，默认 project' },
      importance: { type: 'number', description: '重要性 0-1，默认 0.5' },
    },
    output: {
      schema: { type: 'object', additionalProperties: false, properties: {
        id: { type: 'string', required: true }, record: { type: 'object', required: true } } },
      render: (_args, value: { record: { id: string; kind: string; importance: number } }) => [{
        type: 'text',
        text: `已保存记忆 ${value.record.id}（kind=${value.record.kind}, importance=${value.record.importance}）。后续会话可用 engram_search 召回。`,
      }],
    },
    async execute(args: unknown, exec) {
      const input = args as { content: string; kind: 'fact' | 'preference' | 'decision' | 'episode' | 'skill'; scope?: unknown; importance?: number }
      const scope = scopeOf(input.scope)
      const store = await deps.openStore(scope)
      const record = await store.write({
        scope, kind: input.kind, content: input.content,
        importance: input.importance, sourceSessionId: exec.agent?.id ?? null,
      })
      return { id: record.id, record }
    },
  })

  const search = defineTool({
    name: 'engram_search',
    description: '语义 + 关键词混合检索长期记忆。user 作用域存偏好与通用事实，project 作用域存项目约定与决策。',
    parameters: {
      query: { type: 'string', required: true, description: '检索文本' },
      scope: { type: 'string', enum: ['user', 'project', 'all'], description: '作用域，默认 all' },
      limit: { type: 'number', description: '返回条数上限，默认 8' },
    },
    output: {
      schema: { type: 'object', additionalProperties: false, properties: {
        degraded: { type: 'boolean', required: true }, text: { type: 'string', required: true } } },
      render: (_args, value: { degraded: boolean; text: string }) => [{ type: 'text', text: value.text }],
    },
    async execute(args: unknown) {
      const input = args as { query: string; scope?: unknown; limit?: number }
      const raw = scopeOf(input.scope, 'all' as never) === undefined ? [] : undefined
      void raw
      const scopes: EngramScope[] = input.scope === 'user' ? ['user'] : input.scope === 'project' ? ['project'] : ['user', 'project']
      const queryText = input.query.trim()
      const queryVector = deps.embedder === undefined || queryText === '' ? undefined : (await deps.embedder.embed([queryText]))[0]?.vector
      const results = await Promise.all(scopes.map(async (scope) => {
        const store = await deps.openStore(scope)
        return store.search({ text: queryText, scopes: [scope], limit: input.limit ?? 8 }, queryVector)
      }))
      const merged = results.flatMap(result => result.hits).sort((a, b) => b.score - a.score).slice(0, input.limit ?? 8)
      const degraded = results.some(result => result.degraded)
      const lines = merged.map((hit, index) =>
        `${index + 1}. [${hit.record.scope}/${hit.record.kind}] ${hit.record.content}（id=${hit.record.id}）`)
      const text = degraded ? `（语义嵌入不可用，仅关键词检索）\n${lines.join('\n') || '无命中'}` : lines.join('\n') || '无命中'
      return { degraded, text }
    },
  })

  const timeline = defineTool({
    name: 'engram_timeline',
    description: '按时间范围与主题浏览记忆（倒序）。无参数默认最近 20 条。',
    parameters: {
      scope: { type: 'string', enum: ['user', 'project', 'all'], description: '作用域，默认 all' },
      topic: { type: 'string', description: '主题子串' },
      since: { type: 'string', description: '起始 ISO 时间，如 2026-08-01' },
      until: { type: 'string', description: '结束 ISO 时间' },
    },
    output: {
      schema: { type: 'object', additionalProperties: false, properties: { text: { type: 'string', required: true } } },
      render: (_args, value: { text: string }) => [{ type: 'text', text: value.text }],
    },
    async execute(args: unknown) {
      const input = args as { scope?: unknown; topic?: string; since?: string; until?: string }
      const scopes: EngramScope[] = input.scope === 'user' ? ['user'] : input.scope === 'project' ? ['project'] : ['user', 'project']
      const sinceMs = input.since === undefined ? undefined : Date.parse(input.since)
      const untilMs = input.until === undefined ? undefined : Date.parse(input.until)
      if (input.since !== undefined && Number.isNaN(sinceMs)) throw new Error(`engram_timeline: since 不是合法时间 ${input.since}`)
      if (input.until !== undefined && Number.isNaN(untilMs)) throw new Error(`engram_timeline: until 不是合法时间 ${input.until}`)
      const rows = (await Promise.all(scopes.map(async (scope) => (await deps.openStore(scope)).timeline({
        scopes: [scope], topic: input.topic, since: sinceMs, until: untilMs, limit: 20,
      })))).flat().sort((a, b) => b.createdAt - a.createdAt).slice(0, 20)
      return { text: rows.map(record => `${new Date(record.createdAt).toISOString()} [${record.scope}/${record.kind}] ${record.content}（id=${record.id}）`).join('\n') || '时间线为空' }
    },
  })

  const update = defineTool({
    name: 'engram_update',
    description: '修正一条记忆：写入新条目并把旧条目标记为被取代（链条保留，可审计）。',
    parameters: {
      id: { type: 'string', required: true, description: '要修正的旧条目 id（engram_search 结果里给出）' },
      content: { type: 'string', required: true, description: '修正后的正文' },
      scope: { type: 'string', enum: ['user', 'project'], description: '旧条目作用域' },
      kind: { type: 'string', enum: ['fact', 'preference', 'decision', 'episode', 'skill'], description: '种类，默认继承旧条目' },
    },
    output: {
      schema: { type: 'object', additionalProperties: false, properties: {
        record: { type: 'object', required: true }, superseded: { type: 'string', required: true } } },
      render: (_args, value: { record: { id: string }; superseded: string }) =>
        [{ type: 'text', text: `已写入修正记忆 ${value.record.id}；旧条目 ${value.superseded} 已归档并建立取代链。` }],
    },
    async execute(args: unknown) {
      const input = args as { id: string; content: string; scope?: unknown; kind?: 'fact' | 'preference' | 'decision' | 'episode' | 'skill' }
      const scope = scopeOf(input.scope)
      const store = await deps.openStore(scope)
      const old = await store.get(input.id as never)
      if (old === undefined) throw new Error(`engram_update: 条目 ${input.id} 不存在`)
      const record = await store.update({
        id: input.id as never, scope, kind: input.kind ?? old.kind, content: input.content,
      }, undefined)
      return { record, superseded: input.id }
    },
  })

  const forget = defineTool({
    name: 'engram_forget',
    description: '遗忘一条记忆（软删，用户可从库中恢复）。',
    parameters: {
      id: { type: 'string', required: true, description: '条目 id' },
      scope: { type: 'string', enum: ['user', 'project'], description: '条目作用域' },
    },
    output: {
      schema: { type: 'object', additionalProperties: false, properties: { id: { type: 'string', required: true } } },
      render: (_args, value: { id: string }) => [{ type: 'text', text: `记忆 ${value.id} 已遗忘（软删，可恢复）。` }],
    },
    async execute(args: unknown) {
      const input = args as { id: string; scope?: unknown }
      const store = await deps.openStore(scopeOf(input.scope))
      const record = await store.forget(input.id as never)
      return { id: record.id }
    },
  })

  return [save, search, timeline, update, forget]
}
```

- [ ] **Step 5.3: 写 `src/index.ts`**（插件入口）

```ts
/**
 * dsh-engram：DeepSeek Harness 跨会话长期记忆插件（host 半）。
 * 注册 5 个 engram_ 工具，并在会话开始时注入用户级画像摘要。
 * @module @kenz1117/dsh-engram
 */

import { mkdir } from 'node:fs/promises'
import type { Context } from '@deepseek-ai/cordis'
import z from '@deepseek-ai/schemastery'
import { createUserMessage } from '@deepseek-ai/dsh-llm'
import type { Agent, PreStepDecision } from '@deepseek-ai/dsh-agent'
import { openEngramStore } from './store/sqlite.ts'
import type { EngramStore } from './store/interface.ts'
import { createLocalEmbedder } from './embedder/local.ts'
import type { EngramEmbedder } from './embedder/interface.ts'
import { resolveConfig } from './config.ts'
import type { EngramConfig, ResolvedEngramConfig } from './config.ts'
import { createEngramTools } from './tools/create.ts'
import type { EngramScope } from './types.ts'

/** Cordis 插件名（loader 诊断与注入 source 使用）。 */
export const name = 'dsh-engram'

/** 必需服务：工具注册表。 */
export const inject = ['tools']

export { Config } from './config.ts'
export type { EngramConfig } from './config.ts'

/**
 * 插件体：预热分库与嵌入器，注册工具与画像注入。
 * @param ctx - host 上下文。
 * @param config - cordis.yml 传入的可选配置。
 */
export function apply(ctx: Context, config: EngramConfig = {}): void {
  const resolved: ResolvedEngramConfig = resolveConfig(config)
  void mkdir(resolved.dbDir, { recursive: true, mode: 0o700 })

  // 分库懒打开：user 恒打开；project 库按进程工作目录命名（跨会话同项目共享）。
  const projectDbName = `project-${Buffer.from(process.cwd()).toString('hex').slice(0, 24)}.db`
  const stores = new Map<EngramScope, Promise<EngramStore>>()
  const openStore = (scope: EngramScope): Promise<EngramStore> => {
    const existing = stores.get(scope)
    if (existing !== undefined) return existing
    const path = scope === 'user' ? `${resolved.dbDir}/user.db` : `${resolved.dbDir}/${projectDbName}`
    const created = openEngramStore(path)
    stores.set(scope, created)
    return created
  }

  // 嵌入器可选：下载失败不阻塞插件加载，检索自动降级（fail loud 于首次下载错误日志）。
  const embedderPromise: Promise<EngramEmbedder | undefined> = createLocalEmbedder(resolved.modelCacheDir)
    .catch((error: unknown) => {
      console.warn('[dsh-engram] 嵌入器不可用，检索降级为纯关键词模式：', error)
      return undefined
    })
  void embedderPromise

  ctx.tools.register(...createEngramTools({
    openStore,
    get embedder(): EngramEmbedder | undefined { return undefined },
  }))

  // 画像注入：每轮第一步把用户级 top-N 记忆作为带来源的动态上下文追加。
  registerProfileInjection(ctx, openStore, resolved, embedderPromise)
}

/**
 * agent/pre-step waterfall：step===1 时注入画像。必须调用 next() 委托链路。
 */
function registerProfileInjection(
  ctx: Context,
  openStore: (scope: EngramScope) => Promise<EngramStore>,
  resolved: ResolvedEngramConfig,
  embedderPromise: Promise<unknown>,
): void {
  void embedderPromise
  if (!resolved.injectProfile) return
  ctx.on('agent/pre-step', async (
    { agent, step }: { agent: Agent; step: number },
    next,
  ): Promise<PreStepDecision> => {
    const decision = await next()
    if (step !== 1) return decision
    const store = await openStore('user')
    const top = await store.topActive('user', resolved.profileTopN)
    if (top.length === 0) return decision
    const text = [
      'User memory profile (dsh-engram, cross-session):',
      ...top.map(record => `- [${record.kind}] ${record.content}`),
      'Use engram_search to recall details; use engram_save to persist new facts.',
    ].join('\n')
    return {
      ...decision,
      messages: [
        ...decision.messages,
        createUserMessage({
          content: [{ type: 'text', text }],
          source: { kind: 'plugin', plugin: name, form: 'snapshot', sections: [{ name, text }] },
        }),
      ],
    }
  }, { prepend: true })
}
```

实现时两处必须写对（计划占位即修正）：① `ctx.tools.register(...createEngramTools({ openStore, embedder: await? }))`——`apply` 是同步函数，工具的 `embedder` 依赖必须改为 `ToolDeps.embedder` 支持传入 `Promise<EngramEmbedder | undefined>`，`create.ts` 里 `search` 执行时 `await deps.embedder`；同步删除计划草稿里 `get embedder()` 的占位写法。② `import z from '@deepseek-ai/schemastery'` 仅当入口重新导出 `Config` 时需要；`Config` 在 `config.ts` 定义并经 `export { Config } from './config.ts'` 转出，无需在入口 import。

- [ ] **Step 5.4: 运行验证**

```sh
pnpm typecheck && pnpm test   # 期望：全部通过（config/store/tools）
```

- [ ] **Step 5.5: 提交**

```sh
git add -A && git commit -m "feat: engram_ tools, plugin entry, and profile injection"
```

---

### Task 6: 组合测试、README 与 v0.0.1 提交

**Files:**
- Create: `tests/composition.spec.ts`、`README.md`
- Modify: `package.json`（如依赖版本有变）

- [ ] **Step 6.1: 写 `tests/composition.spec.ts`**（真实组合：插件装载进 cordis Context，断言工具注册与注入监听生效）

```ts
import { mkdtemp } from 'node:fs/promises'
import { tmpdir } from 'node:os'
import { join } from 'node:path'
import { afterEach, beforeEach, describe, expect, it } from 'vitest'
import { Context } from '@deepseek-ai/cordis'
import * as Engram from '../src/index.ts'

let dir: string
let ctx: Context

beforeEach(async () => { dir = await mkdtemp(join(tmpdir(), 'engram-comp-')) })
afterEach(async () => { if (ctx !== undefined) await ctx.dispose() })

describe('plugin composition', () => {
  it('装载后注册 5 个工具，dispose 后卸载', async () => {
    ctx = new Context()
    ctx.plugin({ ...Engram, apply: (c: Context, config?: Engram.EngramConfig) => Engram.apply(c, { ...config, dbDir: dir, injectProfile: false }) })
    await ctx.await()
    const names = ctx.tools.list().map(tool => tool.name)
    for (const expected of ['engram_save', 'engram_search', 'engram_timeline', 'engram_update', 'engram_forget']) {
      expect(names).toContain(expected)
    }
    await ctx.dispose()
    ctx = undefined as never
  })
})
```

实现时按 cordis 实际装载 API 调整装载写法（`ctx.plugin(Engram)` + `Config` 传参形态以 `@deepseek-ai/cordis` 的函数插件装载契约为准；若 `ctx.tools.list()` 名称不同，用 `ctx.tools` 实际读取 API）。断言目标不变：装载后 5 个工具可见、dispose 后消失。

- [ ] **Step 6.2: 写 `README.md`**（含 Model Experience 三小节）

````markdown
# @kenz1117/dsh-engram

DeepSeek Harness 的跨会话长期记忆插件：Agent 在会话与项目之间记住用户偏好、项目约定与经历事实。

## 使用本包

```sh
dsh plugin --profile web add @kenz1117/dsh-engram
```

cordis.yml 无需配置即可使用（默认 `~/.dsh/engram` 分库、开启画像注入）。可选配置：

```yaml
- id: dsh-engram
  name: '@kenz1117/dsh-engram'
  config:
    dbDir: '~/.dsh/engram'      # 分库与模型缓存根目录
    injectProfile: true          # 会话开始注入用户画像摘要
    profileTopN: 8               # 注入条数上限
```

工具：`engram_save` / `engram_search` / `engram_timeline` / `engram_update` / `engram_forget`。

## 理解实现

- 双层分库：`user.db` 全局共享；`project-<cwd>.db` 按工作目录隔离。
- 混合检索：FTS5（trigram，中文友好）+ 本地向量（`Xenova/bge-small-zh-v1.5`，512 维，量化权重）RRF 融合 + 关系边一跳扩展。
- 修正走取代链：`engram_update` 归档旧条目并建立 `supersedes` 边，链条完整可审计；操作日志表记录全部写入/修改/遗忘。
- 嵌入模型首次使用需联网下载（约 50MB），此后完全离线；下载失败时检索自动降级为纯关键词并在结果中标记。

## Model Experience

### Request context and condition

#### What the model sees

会话每轮第一步追加一条 plugin 来源的 user 快照：`User memory profile (dsh-engram, cross-session):` 加用户级记忆列表（默认至多 8 条）。工具调用结果为纯文本行列表（含 id、scope/kind 标注与降级说明）。

#### Token effect

画像注入为条件性固定成本（条数 × 内容长度）；工具 schema 为常驻成本（5 个窄参数工具）。

#### KV Cache effect

画像文本随记忆库内容变化——变化只发生在会话首轮之后的轮次边界，注入条目在同一会话内不变时前缀保持稳定；工具 schema 恒定，不影响前缀。

## Known Limitations and Deferred Work

- **自动摄取未实现** —— 会话结束不从日志提取候选记忆，依赖模型显式调用 `engram_save`；二期在 session 事件流上加摄取钩子（`ingest: off | light | eager`）。
- **飞轮未实现** —— 矛盾检测、蒸馏、衰减调度与 `engram_review/stats/export/distill` 四个审计工具在二期。
- **来源链只有 sessionId** —— 轮次与事件 seq 归属随自动摄取补全。
````

- [ ] **Step 6.3: 全量验证**

```sh
pnpm typecheck && pnpm test && pnpm bundle
ls lib/   # 期望：index.js 与 index.d.ts 存在
```

预判失败与对策：① tsdown dts 与 `allowImportingTsExtensions` 冲突 → tsdown 内部用 rolldown-plugin-dts 处理 `.ts` 相对导入，若报错则把 `dts: true` 换成 `dts: { resolve: false }` 或将源码相对导入改回无后缀；② `node:sqlite` 类型在 `@types/node` 较旧版本缺失 → 升级 `@types/node` 到 ≥22.19 对应版本；③ transformers.js 在 vitest node 环境初始化失败 → e2e spec 已用 `ENGRAM_E2E` 门控，保持默认跳过。

- [ ] **Step 6.4: v0.0.1 提交（设计文档 + 代码同 commit）**

```sh
git add -A
git commit -m "feat: dsh-engram v0.0.1 — cross-session memory with dual-scope SQLite store, hybrid retrieval, and profile injection"
git tag v0.0.1
```

---

## 自审记录

- 设计覆盖：一期五项交付（骨架/存储/嵌入/5 工具/注入）各有任务；二期三期不在本计划（口径与 README 的 Deferred Work 一致）。
- 类型一致性：`MemoryId` 全链路品牌化；`ToolDeps.embedder` 在 Task 5 落定为 `Promise | undefined` 双形态（入口同步 apply 的约束推论）；`openStore(scope)` 签名在 store/tools/inject 三处一致。
- 已知占位修正点已内联标注（search 的 scope IN 列表、recordOps 未用项、create.ts 的 embedder await 形态、组合测试的装载写法），实现者按标注写对，不留 TBD。
